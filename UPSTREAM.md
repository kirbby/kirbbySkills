# Upstream sources

## babysit-pr

`.codex/skills/babysit-pr` is copied from
[`openai/codex`](https://github.com/openai/codex/tree/main/.codex/skills/babysit-pr)
at commit `c98e263fb5365a512bb997a103d6ee8aa14c23e6`.

It is licensed under Apache License 2.0. The source code and accompanying
skill files are preserved so the watcher remains usable.

## ui-ux-pro-max

`skills/ui-ux-pro-max` is a copy of the locally installed `ui-ux-pro-max`
skill as of 2026-09-25, including its data, scripts, tests, and references.
Generated Python caches are excluded. The copied files are unchanged.

The upstream project is
[`nextlevelbuilder/ui-ux-pro-max-skill`](https://github.com/nextlevelbuilder/ui-ux-pro-max-skill).
The installed copy has local changes and no Git metadata identifying its
original upstream revision. It is not represented as a copy of current upstream.
The upstream MIT licence is included in the skill directory, retrieved from
commit `dcc40ff5133ef78276117db0cc34e7b83cc8aeba`.
Existing data provenance and font licence metadata are preserved.

## create-deploy-script

`skills/create-deploy-script` is an unchanged copy of the locally installed
`t3code-app` directory as of 2026-09-25. Its skill name is
`create-deploy-script`. The deployment reference and agent metadata are included.
No original source URL, revision, or separate licence was supplied with this
local skill. The original agent metadata still uses the `t3code-app` name.
